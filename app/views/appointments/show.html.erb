<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) if @appointment.doctor  %><br>
<%= link_to @appointment.patient.name, patient_path(@appointment.patient) if @appointment.patient %><br>
<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %R") %><br>