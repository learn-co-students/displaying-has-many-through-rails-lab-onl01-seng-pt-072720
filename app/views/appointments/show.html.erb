<%= link_to @appointment.doctor_name, doctor_path(@appointment.doctor) if @appointment.doctor  %><br>
<%= link_to @appointment.patient_name, patient_path(@appointment.patient) if @appointment.patient %><br>
<%= @appointment.appointment_datetime.strftime("%B %d, %Y at %R") %><br>