<%= @patient.name %>
<br>
<% @patient.appointments.each do |appt| %>
    <% @doctor = @patient.doctors.find(appt.doctor_id) %>
    <%= link_to @doctor.name, doctor_path(@doctor) %> - <%= link_to appt.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appt) %>
    <br>
<% end %>