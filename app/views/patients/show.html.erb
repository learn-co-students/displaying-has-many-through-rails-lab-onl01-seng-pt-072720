<%= @patient.name %>
<ul>
    <% @patient.appointments.each do |appt| %>
        <li> <%= appt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %><br>
        <%= link_to appt.doctor.name, doctor_path(appt.doctor.id) %> </li><br>
    <% end %>
</ul>
