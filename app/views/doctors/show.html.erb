<h1><%= @doctor.name %></h1>
<ul>
    <%= @doctor.appointments.each do |appt| %>
        <li><%= link_to appt.patient.name, patient_path(appt.patient.id) %>
            <%= appt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %></li><br>
    <% end %>
</ul>